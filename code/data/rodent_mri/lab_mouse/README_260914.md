# 实验室鼠类原始数据

来源会话：`01a09f0a-f236-7ca3-96f1-84bf06cc493e`。原始文件来自旧服务器 `192.168.1.230`，盘点依据为现存的 [nxz_rat_provenance](/home/data2/chk/workspace/2026/09/14/nxz_rat_provenance/INVENTORY.md)。

已复制 **14,587 个文件，19,764,668,842 字节（约 19.76 GB）**；另链接已有的 17,374,752 字节 CEST 原始 ZIP。合计 14,588 个文件均已通过源端与本地 SHA256 比对；没有缺失或多出的文件。原清单中 708 个物理路径的主重建哈希也与本次源端读取一致。

## 文件入口

远端绝对路径 `/home/...` 对应本地 `old_server/home/...`，完整路径映射见 [扫描清单](provenance/scan_catalog_260914.json)。例如远端 `/home/user/data2/RAT/...` 对应 [old_server/home/user/data2/RAT/](old_server/home/user/data2/RAT/)。

- 原生扫描：复制完整扫描目录，保留 `method`、`acqp`、已有的 `fid` / `ser`、`pdata` 等文件；同时复制 study 父目录直属文件，包括 `subject` 和 `AdjStatePerStudy`。
- CEST 原始包：[GluMice.Dl2.zip](old_server/home/qingjia/matlabcode/GluMice.Dl2.zip) 链接到本机已有的 `/home/data2/chk/workspace/2026/09/14/nxz_rat_provenance/sources/GluMice.Dl2.zip`，保留原始压缩包，不展开成处理数据。

## 保留范围与计数

既有清单按主重建 `pdata/1/2dseq` 的 SHA256 去重，共 601 组：

| 分类 | 主重建去重组数 |
|---|---:|
| 脑部 RARE 候选 | 432 |
| 身体 / 肿瘤普通 RARE | 79 |
| 身体 / 肿瘤 RAREVTR | 43 |
| 身体 CEST-RARE，保存在上述 ZIP 中 | 47 |

这些是扫描 / 重建组数，不是独立动物数。为保留采集记录，本次同时保留主重建重复的来源路径：708 个物理扫描路径，加上原清单中 13 个缺少完整主重建的路径，共复制 721 个扫描目录。ZIP 中的 47 组另计，不与 721 个目录相加来推导独立采集数。

重复来源保留原名、原路径和元数据，不因 `2dseq` 一致而假定 FID 也一致。13 个不完整路径在扫描清单中标记；本次不修补或重建。721 个目录中 702 个有 FID / SER，19 个没有；另有 1 个目录的父层没有 `subject`。这些均按现有内容保留，具体可用性见扫描清单。既有清单排除的 19 组模体不收进鼠类采集目录，其排除依据保留在来源清单中。

`liyang_Rat_data.mat`、`MouseData.zip` 内处理后的鼠脑 MAT、训练 PNG、仿真 NIfTI 和 `brain_gt192` 输出不归入原始采集库；来源关系仍可在上游溯源目录查阅。本次复制范围由已有清单明确界定，不代表旧服务器的全部鼠类数据已被穷尽。

## 溯源与核验

- [导入汇总](provenance/import_summary_260914.json)：复制量、链接量、采集文件缺失与既有主重建哈希比对结果。
- [既有来源清单原件](provenance/source_inventory_260914.json)：保留扫描分类、原始路径、重复别名、元数据差异和排除记录。
- [本次扫描路径目录](provenance/scan_catalog_260914.json)：远端与本地映射、缺失重建标记、采集文件是否存在。
- [源端逐文件清单与 SHA256](provenance/remote_manifest_260914.json)：覆盖扫描目录、父层元数据及 CEST 原始 ZIP。
- [本地核验结果](provenance/verification_260914.json)：逐文件比较本地字节数和 SHA256，不解码或处理 MRI 数值。
- 导入操作日志保存在 `code/data/runs/raw_import_260914/`。

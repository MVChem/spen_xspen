# 项目数据

[鼠类 MRI 数据与溯源](rodent_mri/README_260914.md) 收录实验室原始采集文件及公开数据的下载原件。

本机已有数据通过符号链接接入；旧服务器上的采集文件复制到本目录的数据集内。来源、原路径、文件大小和 SHA256 随数据保存。符号链接指向原文件，使用时将输出放在相应代码项目的 `runs/`，不要覆盖输入数据。

`runs/` 保存导入日志。

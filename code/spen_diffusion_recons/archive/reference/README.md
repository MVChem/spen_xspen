# 旧脚本参考

`real/`、`simulation/`、`data/` 保留原来的真实数据批处理、制图和数据下载源码。
这些脚本依赖旧实验目录的布局、清单、MATLAB 环境或机器配置，当前不属于可运行入口。
Python 源码存为 `.py.reference`，MATLAB 源码存为 `.m.reference`，防止误运行到旧路径。
使用前应复制到活跃脚本目录，改成显式输入参数，并将输出设到本项目 `runs/`。

未复制 source_snapshot 中的权重、缓存或实验记录。历史结果请从新项目根 README 的旧资料索引访问。

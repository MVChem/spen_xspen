# 项目工作约定

## 目录用途

- `tmp/`：暂存当前实验进展、展示结果和临时有用的 notes，主要供用户查看。用户觉得有保留价值时，会自行移到其他位置。一般每次开始工作时这里应为空，由用户手动清理；助手不自动清空，也不依赖上次的临时文件持续存在。
- `notes/`：保存用户需要留下的笔记，当前以 Markdown 为主。旧 notes 已按用户要求清空，不重新生成原先的开发说明、迁移清单或通用项目索引。
- `experiments/`：按文件夹展示用户认为重要的实验结果，内容主要来自 `code/<项目>/runs/`。由用户挑选并自行放入，助手不自动搬运或复制 runs 到这里。
- `code/`：存放各个代码项目及其源码。每个项目独立放在 `code/<项目>/`，可以有自己的 `runs/`，用于保存实验记录和运行产物。
- `paper/`：论文源码、参考文献与正式配图。
- `周报/`：周报及相关材料。

## 命名

- notes 文件或笔记文件夹的名称以六位日期 `YYMMDD` 结尾。Markdown 的日期放在扩展名前，例如 `重建观察_260914.md`。
- `experiments/` 下新建的实验结果文件夹同样以 `YYMMDD` 结尾，例如 `重建对比_260914/`。
- `260914` 表示 2026 年 9 月 14 日，使用对应笔记或实验的日期。

## 工作时如何放置内容

- 修改源码时进入相应的 `code/<项目>/`，实验记录及产物写入该项目的 `runs/`。旧脚本默认输出到其他位置时，运行前通过已有输出参数指定到 `runs/`。
- 当前进展、临时笔记和供查看的展示材料先放 `tmp/`；需要生成 notes 文件时，遵守上述日期后缀约定。
- 不把全部运行结果直接堆到 `experiments/`；由用户决定哪些结果值得保留展示。
- 项目约束维护在本文件，目录概览维护在根 `README.md`；临时工作内容不写进这两个文件。
